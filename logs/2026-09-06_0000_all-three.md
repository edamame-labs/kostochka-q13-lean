# 2026-09-06 00:00 all-three — Lean, smallest counterexample, analogue, note, git

## Goal
User answered "all 3": (1) Lean formalisation, (2) smallest counterexample + corrected
characterisation + draft note, (3) git.

## What was done
- git init, two commits so far.
- Installed elan (Lean 4.34.0-rc2) and a Mathlib project `q13lean/` (cache: ~7.8 GB).
- `q13lean/Q13lean/Counterexample.lean`: vertex type `V k = Unit ⊕ Fin m ⊕ Unit ⊕ (Fin k × Fin k × Fin 2)`,
  graph `F k` via `SimpleGraph.fromRel`, lists `L`, `IsSE`, theorems `alpha_condition`,
  `exists_big_fiber` (pigeonhole via `Finset.exists_lt_card_fiber_of_mul_lt_card_of_maps_to`),
  `no_equitable_L_coloring`, `not_SE_L_colorable`, `question13_false`. Builds; no sorry.
  Compile fixes needed: abbrev `A` cannot be `rw`-unfolded; omega needs products generalised;
  `L_b` unfolding lemma to keep `f (b k …)` syntactically stable for omega.
- `scripts/cegis.py`: SAT (pysat/CaDiCaL) CEGIS for defeating list assignments; blocking
  clauses for all single-vertex recolourings under colour permutations fixing [k].
  Bug found and fixed: `search.py` ran its `main()` on import (no `__main__` guard).
- notes/STRUCTURE.md: leaf-extension lemma, twin-leaf lemma, minimal-counterexample
  corollary (k ∤ n ⇒ no twin leaves, no K₂ component), Proposition 4 (general obstruction).
- notes/ANALOGUE.md: conditions (C)+(G), Conjecture A, Question B.
- paper/note.tex compiled with tectonic (installed via brew) → paper/note.pdf.

## Results
- Exhaustive (selc): no counterexample for k=3 n≤12 (palette 4), n≤8 (palette 5);
  k=4 n≤11 (palette 5); k=5 n≤10 (palette 6).
- CEGIS: see scripts/out/cegis_*.txt (running at end of session).

## Dead ends / what failed and why
- First CEGIS timing (36 s for n=8) was an artefact of the import bug; real time 3.5 s.
- Could not find an obstruction mechanism with k instead of k² stuck vertices per unit
  of slack inside a forest (needs a common neighbour of two centres → cycle).

## Open questions / next steps
- Push CEGIS to n ≈ 12–14 with palette 6; decide whether a counterexample below 57 exists.
- Attack Conjecture A / Question B; start with star forests (K_{1,m₁}+K_{1,m₂}), where the
  exact SE condition should be a finite b-matching computation.
- Formalise the tree variant and/or Theorem 27 in Lean if desired.
